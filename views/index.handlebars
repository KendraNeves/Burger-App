<h1>Eat-Da-Burger!</h1>
<h4>Enter a burger you're craving then smash that "Submit" button!</h4>

       


{{!-- column of burgers that have been ordered (but not devoured), needs more refactoring --}}
<ul>
  {{#each burgers}}
  {{/each}}
</ul>



{{!-- Code that needs to be refactored        --}}
       
        <!-- <h2>Cats that are not sleepy!</h2>
        <ul>
          {{#each cats}}
            {{#unless sleepy}}
              {{> cats/cat-block sleep=true}}
            {{/unless}}
          {{/each}}
        </ul>

        <h2>Cats that are sleepy!</h2>
        <ul>
          {{#each cats}}
            {{#if sleepy}}
              {{> cats/cat-block sleep=false}}
            {{/if}}
          {{/each}}
        </ul> -->
 {{!-- =============================================================        --}}


{{!-- form and submit button --}}
<form class="create-form">

  <div class="form-group">
    <label for="ca"></label>
    <input type="text" id="ca" name="name">
  </div>

  <button type="submit">Submit</button>

</form>
